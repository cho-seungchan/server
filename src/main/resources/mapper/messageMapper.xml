<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.pickcourse.mapper.MessageMapper">

    <!-- 메시지 저장 -->
    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="messageId" keyColumn="ID">
        INSERT INTO TBL_MESSAGE (ID, MESSAGE_ALL_CONTENT, MESSAGE_ALL_DATE)
        VALUES (SEQ_MESSAGE.NEXTVAL, #{content}, SYSDATE)
    </insert>

    <!-- 메시지 조회 -->
    <select id="selectMessageById" resultType="com.app.pickcourse.domain.vo.MessageVO">
        SELECT ID AS messageId, MESSAGE_ALL_CONTENT AS content, MESSAGE_ALL_DATE AS messageDate
        FROM TBL_MESSAGE
        WHERE ID = #{messageId}
    </select>

    <!-- 메시지 조회 -->
    <select id="selectLastInsertedId" resultType="Long">
        SELECT SEQ_MESSAGE.CURRVAL FROM DUAL
    </select>

</mapper>
